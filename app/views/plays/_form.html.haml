= form_with(model: play, local: true) do |form|
  - if play.errors.any?
    #error_explanation
      %h2
        = pluralize(play.errors.count, "error")
        prohibited this play from being saved:
      %ul
        - play.errors.full_messages.each do |message|
          %li= message
  .actions
    = form.submit
